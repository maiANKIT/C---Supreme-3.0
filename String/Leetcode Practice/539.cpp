#include <iostream>
#include <string>
#include <algorithm>

using namespace std;

int main()
{

    string timePoints;
    cout << "enter the time: ";
    cin >> timePoints;

    int h = 0;
    int m = 0;
    int min = 0;

    for (int i = 0; i < (timePoints.size() / 2); i++)
    {

        min =
    }

    for (int i = 3; i < timePoints.size() - 1; i++)
    {
        if (i = 3)
        {
            min = stoi(timePoints[i]);
        }
    }

    return 0;
}