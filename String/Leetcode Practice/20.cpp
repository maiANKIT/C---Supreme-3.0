#include <iostream>
#include <vector>
#include <cmath>
#include <string>
#include <algorithm>

using namespace std;

int main()
{

    string s;
    cout << "enter the string: ";
    cin >> s;

    bool x = 1;
    int i = 0;
    int j = s.size() - 1;

    if (s.size() % 2 != 0)
    {
        x = 0;
    }
    else
    {

        while (i < j)
        {
            if (s[i])
        }
    }

    cout << x << endl;

    return 0;
}