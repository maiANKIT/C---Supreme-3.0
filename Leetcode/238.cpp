#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int main()
{

    int n;
    cout << "enter the size: ";
    cin >> n;

    vector<int> nums(n);
    cout << "enter the values: ";
    for (int i = 0; i < nums.size(); i++)
    {
        cin >> nums[i];
    }

    int product = 1;
    vector<int> product;
    int i = 0;
    int j = 0;
    while (j < nums.size())
    {

        product = product *
    }

    return 0;
}