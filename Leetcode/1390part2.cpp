#include <bits/stdc++.h>

using namespace std;

int main()
{

    int n;
    cout<<"enter size: ";
    cin>>n;

    vector<int> nums(n);
    cout<<"enter values: ";
    for(int i = 0; i<nums.size(); i++){
        cin>>nums[i];
    }

    int sum = 0;

    for(int i = 0; i<nums[i]; i++){

        int count = 1;

        int j = 2;

        while(nums[i] % j == 0){
            count++;
            if(count>2)
        }

        

    }

   return 0;
}